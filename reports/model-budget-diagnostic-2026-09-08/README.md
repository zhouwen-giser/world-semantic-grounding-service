# 模型预算专项诊断结果（2026-09-08）

## 结论

用户授权增加到合适预算并明确允许重放历史输入后，以 **180 秒**预算完成一次模型专项诊断。结果为 **SUCCEEDED，单次尝试，54.571 秒**（包括 Schema 初始化等脚本总耗时 54.654 秒）。没有重试、没有模型 HTTP 错误或输出校验失败。

慢点位于等待模型服务响应：响应头等待 **54.559 秒**，响应体完成 **54.565 秒**，随后解析校验约 **6 毫秒**。服务报告 prompt_tokens=2257、completion_tokens=2943、reasoning_tokens=2882，即约 **97.9% 的生成 token 属于推理 token**，其余 61 个生成 token。该比例说明本次有大量推理生成工作，但客户端没有服务端排队计时，不能据此量化推理和排队各占多少时间。

本轮排除了 WSGS 重试、响应体读取或 JSON/Schema 校验占用几十秒的情况。它支持模型响应延迟及推理生成开销是当前性能排查重点。此前生产约 90 秒预算超时仍是事实；本轮 54.571 秒未触及旧预算，所以**不能声称提高预算本身解决了问题**，也不能倒推上次失败必定单次尝试或必定由推理开销单独造成。

## 实际输入与执行边界

- 源任务：grounding-3a726139-381d-4209-86f7-4bf13b258439。
- 正式计时：2026-09-08T13:06:26.700Z 至 13:07:21.354Z。
- 输入摘要：sha256:3a5f71da0c46c72019af8635b38bcf6767e59802d13a7da7bf600e4d3407dfac。
- 输出摘要：sha256:857e6f67732f716665cf79ed91f94f81494308efc3a8e384975cc52d8dd77d61。
- 提示词版本：wsgs-domain-semantic-frame/1.0.3；原 CHAT_COMPLETIONS_STRICT 模式、maxRetries=1。
- 重放前只读事务校验原文仍在保留期、AAD、原文哈希及检查点 state_hash，从原 source 与 DETERMINISTIC_PARSE 恢复生产同一入参。模型、地址、凭证、提示词、Schema 保持原实例配置，只有本次进程 timeout=180000。
- 实际逐次计时和脱敏回执见 model-timing.jsonl。不输出原文、模型响应正文或 API key。
- 没有新建 WSGS 业务任务、修改旧结果或检查点，没有调用历史/T3。本次不是全链路验收，T2/T3/T4 状态不变。
- 临时诊断容器自动移除，临时0600环境文件已在finally中删除；API/Worker保持停机，见 instance-state.txt。
- 未永久修改 .env，公开请求的120秒上限及生产任务预算策略保持原样。180秒仅用于此次独立诊断，不应把该值写入普通 Worker 后宣称任务可超出北向上限。

## 可复用入口与验证

validation/scripts/diagnose-model-budget.mjs <groundingId> 180000。允许诊断预算范围 1–300 秒；保持生产适配器与 Schema 校验，记录响应头、首响应体字节、完整响应、token计数及最终回执。脚本语法检查通过，且已实际完成上述模型调用。没有生产代码改动，本轮未重复全仓或数据库集成测试。

初始自动审批拒绝后已取得用户明确授权。诊断预检修正了脚本中的 sha256 前缀比较，并补齐生产 Schema 初始化；两次预检均未进入fetch，未发模型请求。正式计时文件只包含实际调用轮次。

## 后续定位方向

若继续优化，应先核对当前模型服务是否支持限制/关闭推理模式，并用同输入的受控对照评估语义质量与耗时；不能未经验证加入服务不支持的参数。若仍需解释服务端耗时波动，应通过本轮请求关联获取脱敏的排队、首 token、生成耗时，不能将客户端等待时间全部认作GPU计算时间。单纯放宽等待只能容纳更慢响应，不会降低实际模型延迟。
